require 'spec_helper'

describe ReviewsController do
  describe "POST create" do
    #  this doesn't work yet
    #
    # it "should redirect user to review page of the video" do
    #   post :create, video_id: @video, review: { user_id: session[:user_id], video_id: @video, rating: 5, max_rating: 5, review: "This movie was a great movie!" }

    #   response.should redirect_to(video_path)
    # end
  end
end
